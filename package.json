{
  "name": "first-project",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch --env-file=.env --experimental-strip-types --experimental-sqlite ./src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A TODO application that uses Node.js backend SQlite database and JWT authentication",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
